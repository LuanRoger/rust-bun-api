{
  "name": "rust-bun-api",
  "private": true,
  "scripts": {
    "build": "bun run --filter=\"*\" build",
    "dev": "bun run --filter=\"*\" dev",
    "start": "bun run --filter=\"*\" start",
    "test": "bun run --filter=\"*\" test",
    "check": "ultracite check .",
    "format:write": "ultracite fix && turbo run format"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "typescript": "catalog:",
    "ultracite": "^7.1.5"
  },
  "packageManager": "bun@1.3.9",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "typescript": "^5.9.3",
      "@types/bun": "^1.3.9"
    }
  }
}
